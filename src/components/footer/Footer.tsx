import Button from '../button';
import Logo from '../logo';

const Footer = () => (
  <div className="bg-navyBlue flex items-center justify-between px-[16px] py-[36px]">
    <Logo variant="light" />
    <Button color="gradient_2" size="normal" />
  </div>
);

export default Footer;
